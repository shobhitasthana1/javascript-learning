// Use ternary operator:
// Let score = 40.
// If score > 35 → “Pass” else “Fail” using a ternary.

let score = 40;

// ternary operator
let result = (score > 35) ? "Pass" : "Fail";

console.log(result);
